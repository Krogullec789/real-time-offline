# Frontend

React + TypeScript client for the Real-Time Offline Kanban Board.

Useful commands:

```powershell
npm install
npm run dev
npm run lint
npm run build
npm test
```

Runtime URLs are configured through `.env`; see `.env.example`.
